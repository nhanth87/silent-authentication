<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>et.restlink</groupId>
  <artifactId>silent-auth-sas</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>Silent Auth SAS</name>
  <description>CAMARA Number Verification over the Restlink Silent Auth SAS
    (Quarkus + micro-jainslee). Fail-closed Resolver -> Verifier -> Policy.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>
    <quarkus.platform.version>3.37.3</quarkus.platform.version>
    <microjainslee.version>1.2.0-SNAPSHOT</microjainslee.version>
    <surefire.version>3.5.4</surefire.version>
    <jss7.version>9.2.8-j25</jss7.version>
    <corsac-diameter.version>10.0.0-41-SNAPSHOT</corsac-diameter.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.quarkus.platform</groupId>
        <artifactId>quarkus-bom</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-jackson</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-arc</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-jdbc-h2</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-jdbc-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-hibernate-orm-panache</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-elytron-security-common</artifactId>
    </dependency>
    <!-- Provides the MicroSleeContainer CDI bean (MicroJainsleeProducer). -->
    <dependency>
      <groupId>com.microjainslee</groupId>
      <artifactId>adapter-quarkus</artifactId>
      <version>${microjainslee.version}</version>
    </dependency>
    <dependency>
      <groupId>com.microjainslee</groupId>
      <artifactId>jainslee-core</artifactId>
      <version>${microjainslee.version}</version>
    </dependency>
    <dependency>
      <groupId>com.microjainslee</groupId>
      <artifactId>jainslee-api</artifactId>
      <version>${microjainslee.version}</version>
    </dependency>
    <dependency>
      <groupId>com.microjainslee</groupId>
      <artifactId>jainslee-apt</artifactId>
      <version>${microjainslee.version}</version>
      <optional>true</optional>
    </dependency>

    <!-- P2: real MAP transport (jSS7 coral-valley) -->
    <dependency>
      <groupId>org.restcomm.protocols.ss7.config</groupId>
      <artifactId>ss7-config</artifactId>
      <version>${jss7.version}</version>
    </dependency>
    <dependency>
      <groupId>org.restcomm.protocols.ss7.map</groupId>
      <artifactId>map-api</artifactId>
      <version>${jss7.version}</version>
    </dependency>
    <dependency>
      <groupId>org.restcomm.protocols.ss7.map</groupId>
      <artifactId>map-impl</artifactId>
      <version>${jss7.version}</version>
    </dependency>

    <!-- P2: real Diameter S6a / SWx transport (corsac-diameter) -->
    <dependency>
      <groupId>com.mobius-software.protocols.diameter</groupId>
      <artifactId>diameter-api</artifactId>
      <version>${corsac-diameter.version}</version>
    </dependency>
    <dependency>
      <groupId>com.mobius-software.protocols.diameter</groupId>
      <artifactId>diameter-impl</artifactId>
      <version>${corsac-diameter.version}</version>
    </dependency>

    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>io.quarkus.platform</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <version>${quarkus.platform.version}</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
              <goal>generate-code</goal>
              <goal>generate-code-tests</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.0</version>
        <configuration>
          <release>25</release>
          <annotationProcessorPaths>
            <path>
              <groupId>com.microjainslee</groupId>
              <artifactId>jainslee-api</artifactId>
              <version>${microjainslee.version}</version>
            </path>
            <path>
              <groupId>com.microjainslee</groupId>
              <artifactId>jainslee-apt</artifactId>
              <version>${microjainslee.version}</version>
            </path>
            <path>
              <groupId>org.apache.logging.log4j</groupId>
              <artifactId>log4j-api</artifactId>
              <version>2.23.1</version>
            </path>
            <path>
              <groupId>org.apache.logging.log4j</groupId>
              <artifactId>log4j-core</artifactId>
              <version>2.23.1</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire.version}</version>
      </plugin>
    </plugins>
  </build>
</project>
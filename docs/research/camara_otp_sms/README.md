# CAMARA OTP SMS (OTPValidation) — snapshot nguồn

- **Upstream:** `camaraproject/OTPValidation`, release **r3.2** (Fall25),
  API version **1.1.1**, `x-camara-commonalities: 0.6`.
- **File:** `one-time-password-sms-r3.2.yaml` — tải nguyên trạng từ
  `https://raw.githubusercontent.com/camaraproject/OTPValidation/r3.2/code/API_definitions/one-time-password-sms.yaml`
  (2026-09-09).
- **License của spec:** Apache-2.0 (CAMARA). **Không** relicence theo SPDX kép của
  repo này; chỉ trích dẫn/tóm tắt, không dán thêm đoạn spec dài vào tài liệu của ta.
- Ghi chú: tên repo upstream là `OTPValidation` (không phải `OneTimePasswordSMS`);
  base path của API vẫn là `{apiRoot}/one-time-password-sms/v1`.
- Phân tích + phương án tích hợp SAS: `otp-sms-flow-analysis.md` (cùng thư mục).
